# oo7-daemon 0.7.0~alpha falls back to a CLI prompter unless WAYLAND_DISPLAY is
# in its own environment, which niri imports on its way to graphical-session.target.
# TODO: drop once Fedora ships https://github.com/linux-credentials/oo7/pull/558
[Unit]
After=graphical-session.target
