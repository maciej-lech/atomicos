# oo7-daemon only ships the systemd alias, so clients racing its startup get
# ServiceUnknown instead of waiting for it
[D-BUS Service]
Name=org.freedesktop.secrets
Exec=/bin/false
SystemdService=dbus-org.freedesktop.secrets.service
